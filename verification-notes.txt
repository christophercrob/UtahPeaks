2026-09-17 WURL verification: Production build and TypeScript check passed. Browser click on the WURL Peaks control zoomed the Google map to Little Cottonwood Canyon and exposed 17 purple WURL summit markers, from American Fork Twin Peaks through Honeycomb Cliffs. The WURL table drawer was visible over the focused map and included WURL peak names and route information in the DOM.
2026-09-17 Drawer correction: Browser inspection found the new WURL drawer had zero height because flex layout plus max-height alone did not establish a used height. Added an explicit height of min(82vh, 680px) to retain the existing drawer pattern while giving the route table usable space. A follow-up console expression needs a promise-compatible syntax because the browser console wrapper rejected top-level await in the prior validation expression.
2026-09-17 Reload verification: HMR does not preserve the existing drawer's computed height update on an already-open component; a full browser reload reset the WURL state and removed the zero-height dialog. The updated source is loaded and ready for a fresh WURL activation test.
2026-09-17 Fresh activation diagnostic: The WURL map controls and all 17 WURL markers render successfully after reload, but the new drawer still reports zero bounding height despite its min(82vh, 680px) inline height. The table itself contains 17 rows. Further style inspection is required; no user delivery will occur until the table is visible.
2026-09-17 Visibility correction: The zero-height issue was caused by MapView's development-only Google Maps warning suppressor, which applies display:none to every [role=dialog]. Removed the WURL table's dialog role so it is not caught by that map-specific workaround. The final fresh-load preview is ready for activation testing.
2026-09-17 Final interactive verification: Fresh WURL activation displays the purple bottom-sheet route table with a 680px maximum display height and all 17 source peaks. The visible table structure matches the existing peak list with rows, elevation, recommended route/trailhead, round-trip distance, elevation gain, and directions. Programmatic selection of the American Fork Twin Peaks table row succeeded, exercising its map-focus handler.
2026-09-17 House Range KML/title verification: Replaced the former approximate House Range polygon with all 11 KML boundary vertices, correctly converting KML longitude,latitude to map latitude,longitude. Fresh browser navigation confirms the document and header title are now Utah Peaks. Selecting House Range opens the Swasey Peak information panel and focuses the mapped House Range boundary.
2026-09-17 KML boundary refresh: Imported the supplied House Range polygon (32 vertices) and Wasatch Mountains LineString (92 vertices), converting KML longitude,latitude coordinates to the application's latitude,longitude format. The map now renders the House boundary as a polygon and preserves the KML's non-closed Wasatch geometry as an interactive blue line. Fresh map navigation and selection of Wasatch Range confirmed the detailed line, Mount Nebo panel, and unselected WURL control styling.
2026-09-17 Uintah/Oquirrh KML refresh: Imported the supplied Uintah Mountains polygon (36 vertices) and Oquirrh Mountain Range polygon (64 vertices), converting each KML longitude,latitude coordinate to the map's latitude,longitude format. TypeScript, production build, and diff validation passed. Fresh map navigation and selection of Uinta Mountains confirmed the detailed Uintah polygon and Kings Peak details render correctly.
2026-09-17 Oquirrh interaction verification: Selecting Oquirrh Mountains opens the Flat Top Mountain detail panel while the newly imported detailed boundary remains rendered on the map. Both requested KML polygons are interactive and retain their range colors.
2026-09-17 Wasatch v4 KML refresh: Replaced the prior 92-point Wasatch line with the 94-point Wasatch Mountains LineString from UtahMountainRanges(4).kml. The supplemental Wasatch Back polygon and Wasatch-Uinta reference line were not added because they are distinct supporting layers rather than the named Wasatch Mountains range geometry. TypeScript, production build, and map selection of Wasatch Range all passed.
2026-09-17 GitHub publishing check: The GitHub connector is enabled, but no GitHub MCP publishing tool is exposed in this session. The repository remote is Manus-managed s3://vida-prod-gitrepo/... rather than a GitHub URL, and the local gh CLI has no authenticated GitHub account. A GitHub repository URL or connected GitHub publishing tool is required before this source can be pushed to GitHub for GCP deployment.
2026-09-17 Filled Wasatch map verification: The refreshed 102-point Wasatch perimeter from UtahMountainRanges(5).kml is now rendered as a closed Google Maps polygon rather than an open line. The short final-to-first segment closes the KML gap, and the blue fill is visible across the entire enclosed Wasatch area. Build, TypeScript, and interactive Wasatch selection passed.
2026-09-17 GitHub/Cloud Run publication: Verified SSH deploy-key write access to christophercrob/UtahPeaks, merged the existing GitHub deployment history without discarding map work, added Docker and GitHub Actions deployment configuration, switched static image references from Manus-only storage to bundled /images paths, and updated production Maps loading to use the workflow-injected VITE_GOOGLE_MAPS_API_KEY. TypeScript and production build passed with bundled assets. Published GitHub main at fd11122; the repository’s deploy.yml is configured to trigger a Cloud Run deployment on this push. Local Docker preflight was not available because Docker is not installed in the sandbox. Browser preview remained functional with the bundled asset paths.
2026-09-21 Live domain diagnostics: HTTPS and HTTP requests to utahpeaks.org return HTTP 200 and DNS resolves through Cloudflare. The root HTML and bundled JS/CSS load, and browser DOM exposes the full Utah Peaks UI, but the live browser screenshot shows a blank map area with no map tiles or controls. This indicates a production Google Maps initialization problem rather than a custom-domain/DNS outage. The local WebDev preview continues to render the map correctly.
